{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksJson": {
      "excludeFiles": ["app/**", "node_modules/**", ".github/**", "*.md", "*.json", "!package.json"],
      "installCommand": "npm config set registry https://registry.npmmirror.com && npm install",
      "buildCommand": "cd backend && npm config set registry https://registry.npmmirror.com && npm ci && npx prisma generate && npm run build",
      "startCommand": "cd backend && npm run start"
    }
  },
  "deploy": {
    "numReplicas": 1
  }
}
